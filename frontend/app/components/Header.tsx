import { Type } from "lucide-react";

export default function Header() {
  return (
    <header className="flex items-center justify-center px-10 py-5 border-b border-b-border">
      <div className="font-bold text-primary text-xl flex gap-2 items-center font-mono">Timeline</div>
    </header>
  );
}
